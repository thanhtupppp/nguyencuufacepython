# P1.2.2 — FastAPI HTTP + WebSocket transport

Status: DONE (implementation gate; CI status still requires verification)

## Objective
Expose the canonical RecognitionEvent through HTTP and WebSocket without allowing transport clients to bypass biometric validation.

## Implementation
- `app/main.py` provides `GET /healthz`, `POST /v1/events`, and `WS /v1/events/ws`.
- HTTP and WebSocket both validate the same Pydantic `RecognitionEvent` contract.
- Event IDs are idempotent within the process; a duplicate returns the original canonical payload.
- No inference, person matching, quality, or liveness decision is performed by the transport layer.

## Verification
Tests exist for health, valid HTTP ingestion, invalid confirmed events, duplicate HTTP events, and WebSocket serialization. CI must still be checked before claiming the task fully verified.

## Completion criteria
- HTTP implementation present. DONE.
- WebSocket implementation present. DONE.
- Duplicate event behavior is deterministic within one process. DONE.
- Transport layer cannot manufacture a confirmed `person_id` without passing the RecognitionEvent schema. DONE.
- Production persistence/idempotency is explicitly deferred to P1.2.3.

## Next task
P1.2.3 — add durable event persistence/idempotency using PostgreSQL, then implement the MQTT adapter against the same canonical payload.
