# P0.4.28 — CI collection regression hardening

## Status
IMPLEMENTED — fresh CI execution pending.

## Finding
CI run #35 on commit `15bc376` failed during pytest collection before unit/integration execution. Two independent collection issues were observed:

1. repository-root `src` imports were not available during collection;
2. `app.main` intentionally requires `DATABASE_URL` for the production application, but CI did not provide it during unit-test collection.

## Fix
- `pytest.ini` now declares `pythonpath = .` so `src.*` imports are deterministic during collection.
- CI now provides `DATABASE_URL` pointing at its PostgreSQL service.

## Goal
Restore a clean test gate without weakening fail-closed production behavior.

## Verification
Run GitHub Actions CI on the fix commit and require:
- unit tests pass;
- PostgreSQL/pgvector integration tests run instead of being skipped by collection failure;
- no `ModuleNotFoundError: No module named 'src'`;
- no `DATABASE_URL must be set` collection error.

## Completion criteria
PASS only with a completed successful CI run on the fix commit. Do not infer PASS from the code diff alone.

## Next
After CI passes, return to the highest-value blocked execution gate: real licensed SCRFD + ArcFace artifacts and authorized dataset bytes. Retrieval architecture remains benchmark-only until real PostgreSQL measurements are captured.
