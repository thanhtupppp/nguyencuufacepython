# P1.2.1 — FastAPI contract implementation

Status: ACTIVE

## Objective
Implement and test the canonical recognition-event boundary without connecting real biometric inference yet.

## What changed
- Added `app/fastapi_contract.py` with a Pydantic event model.
- Added `tests/test_fastapi_contract.py` covering confirmed-event invariants and timezone-aware timestamps.

## Verification
Run the repository test suite. The confirmed state must require `person_id`, model/embedding versions, quality, liveness, similarity, margin, and at least one confirmation frame.

## Completion criteria
- Contract tests pass in CI.
- Invalid confirmed events are rejected deterministically.
- The contract remains transport-neutral.

## Next task
Add the minimal FastAPI application and HTTP endpoints, then add WebSocket serialization tests. MQTT adapter follows after HTTP/WS contract stability.
