# P0.4.21 — Embedding contract fail-closed

## Mục tiêu
Ngăn embedding lỗi (NaN/Inf, zero-vector, sai dimension, dtype ngoài contract) đi vào recognition hoặc pgvector. ArcFace/InsightFace baseline phải xuất vector 512-D, finite, L2-normalized.

## Cách kiểm chứng
- Unit test vector hợp lệ được chấp nhận.
- NaN/Inf bị từ chối.
- Zero/near-zero norm bị từ chối.
- Sai dimension bị từ chối.
- Batch có một vector lỗi bị từ chối toàn batch.

## Repo/model liên quan
- `src/recognition/base.py`
- `src/recognition/arcface.py`
- InsightFace ArcFace baseline, embedding contract 512-D.

## Tiêu chí hoàn thành
1. Contract validator được dùng trước khi embedding rời recognition layer.
2. Không có NaN/Inf/zero vector đi xuống database.
3. Tests pass trong CI.
4. Không thay model hoặc threshold.

## Bước kế tiếp
P0.4.22 — lock real ArcFace/SCRFD model artifact + provenance/SHA và tạo inference smoke test trên model thật.
