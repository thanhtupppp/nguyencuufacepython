# P0.4.27 — ONNX Graph Contract + CPU Smoke Inference

Status: IMPLEMENTED — execution pending verified model artifacts.

## Objective
Turn the acquired SCRFD + ArcFace artifacts into a machine-verifiable runtime receipt before using them for quality or recognition benchmarks.

## Scope
- Inspect ONNX graph inputs/outputs from the exact SHA-pinned files.
- Record input names, shapes, dtypes, output names, shapes and opset/runtime metadata.
- SCRFD: verify detector produces boxes and 5-point landmarks on a controlled face image.
- ArcFace: verify 112x112 aligned-face input and 512-D finite output; apply the project L2-normalization contract and verify unit norm.
- Run CPU inference only for the first receipt so results are portable across PC/Raspberry Pi-class environments.
- Emit a receipt binding model SHA-256 to graph contract and smoke-test result.

## Verification
Controlled execution requires the exact artifacts from P0.4.26. Do not substitute another checkpoint or mirror without updating provenance and SHA.

Expected ArcFace contract for the pinned buffalo_l candidate is `w600k_r50.onnx`, input equivalent to `[N,3,112,112]`, and 512-D recognition output. The actual ONNX graph remains authoritative and must be recorded rather than assumed.

Expected SCRFD contract is `det_10g.onnx`; actual graph outputs and shapes must be inspected and recorded.

## Acceptance
- Artifact SHA matches the P0.4.26 receipt.
- ONNX graph inspection succeeds on CPU.
- SCRFD returns valid detections and five landmarks for the controlled positive sample.
- ArcFace returns finite 512-D embedding for a valid aligned sample.
- Embedding passes the existing fail-closed embedding validator.
- A JSON receipt contains model SHA, graph contract, runtime version, and smoke-test result.
- No model is marked production-approved by this task.

## Next
P0.2.2 execution: run SCRFD over the authorized real-image calibration dataset, produce quality observations, and calibrate the deterministic quality gate without touching the locked test split.
