# P0.4.25 — Official Artifact Acquisition Receipt

Status: IMPLEMENTED — execution pending artifact download/hash.

## Objective
Make acquisition of the exact official InsightFace v0.7 `buffalo_l.zip` reproducible and fail-closed. Never invent a model SHA-256.

## Locked upstream asset
- Release: `v0.7`
- Asset: `buffalo_l.zip`
- Official URL: `https://github.com/deepinsight/insightface/releases/download/v0.7/buffalo_l.zip`
- Release asset size observed from GitHub: 288,621,354 bytes.
- Expected extracted files: `det_10g.onnx`, `w600k_r50.onnx`.

## Verification
Run:

```bash
python tools/acquire_insightface_v07_models.py
```

The script:
1. downloads only the official release asset;
2. verifies the observed package size;
3. computes package SHA-256 from downloaded bytes;
4. extracts only the two locked model files;
5. computes SHA-256 for each extracted model;
6. writes `models/artifacts/buffalo_l_receipt.json`.

## Acceptance
- Package download succeeds.
- Package size matches the pinned release metadata.
- Both expected ONNX files exist.
- Package and individual file SHA-256 values are recorded from actual bytes.
- Receipt is reproducible by another operator.
- License remains `research-only-until-commercial-license-is-confirmed` until explicit commercial permission is obtained.

## Next
Use the receipt's exact files for P0.4.24 ONNX graph inspection and CPU smoke tests. Do not mark production-approved from acquisition alone.
