# P0.1.2 — YuNet + SFace benchmark gate

Status: DATASET_BLOCKED. Model artifacts are now pinned with SHA256 and verified usable licenses; the A/B benchmark remains blocked only until an authorized identity-labeled evaluation image manifest is available.

## Objective
Compare YuNet + SFace with the SCRFD + ArcFace reference under the same alignment, enrollment/query split, pair manifest, recognition metrics, and runtime measurements.

## Pinned artifacts
- YuNet `face_detection_yunet_2023mar.onnx` — SHA256 `8f2383e4dd3cfbb4553ea8718107fc0423210dc964f9f4280604804ed2552fa4` — MIT.
- SFace `face_recognition_sface_2021dec.onnx` — SHA256 `0ba9fbfa01b5270c96627c4ef784da859931e02f04419c829e83484087c34e79` — Apache-2.0.

## Current result
No empirical recognition result is claimed yet. Model reproducibility is unblocked; authorized evaluation data is still required.

## Verification
1. Validate downloaded artifacts against the pinned SHA256 values.
2. Pin an authorized image manifest with identity IDs, enrollment/query split, and locked test split.
3. Run the existing A/B protocol.
4. Store versioned JSON/Markdown results.
5. Record runtime p50/p95/p99 and detection slices.

## Completion criteria
- reproducible artifact manifest;
- authorized dataset manifest;
- genuine/impostor metrics including FAR/FMR/FNMR and TAR where sample count permits;
- detection slices and runtime p50/p95/p99;
- explicit architecture decision.

## Decision rule
Do not replace SCRFD + ArcFace unless the challenger has usable licensing, meets the false-match target, and provides a material deployment advantage without unacceptable accuracy loss.

## Next task after benchmark
Run real-image quality-gate calibration using the selected recognition model; calibrate thresholds on validation only and evaluate once on a locked test set.
